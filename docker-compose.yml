version: '3.8'

services:
  docs:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/usr/src/app
      - bundle_cache:/usr/local/bundle
    environment:
      JEKYLL_ENV: development
